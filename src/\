// ASSEMBLY PARSER
#ifndef PARSER_H
#define PARSER_H

#include <stdbool.h>
#include <stdio.h>

void read_file(const char* file);
static bool verify_assembly();



void read_file(const char* file){
    if (file != nullptr){
        FILE* file = fopen(read_file); 
        if (!file){
            printf("Failed to open file");
        }
        
        while((c = fgetc(file)) != EOF){

        }


        fclose();
    }

}

#endif
